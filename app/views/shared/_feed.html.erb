<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %>
  </ol>
  <div class="pagy mt-3">
    <%= paginate @feed_items, theme: "twitter-bootstrap-4",
      params: { controller: :static_pages, action: :home } %>
  </div>
<% end %>
