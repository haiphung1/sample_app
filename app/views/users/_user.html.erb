<li class="list-group-item">
  <%= gravatar_for user %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("static_pages.delete"), user, method: :delete, data: {confirm: t("static_pages.confirm")} %>
  <% end %>
</li>
